spring:
    application:
        name: ecomerce

    datasource:
        url: jdbc:mysql://dv:3306/ecomerce_db
        username: root
        password: root

    jpa:
        hibernate:
            ddl-auto: update
        database-platform: org.hibernate.dialect.MySQL8Dialect